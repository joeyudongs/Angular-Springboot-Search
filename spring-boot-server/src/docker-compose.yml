services:
  db:
    image: postgres:16
    container_name: userlab-postgres
    environment:
      POSTGRES_DB: userlab
      POSTGRES_USER: userlab
      POSTGRES_PASSWORD: userlab
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
